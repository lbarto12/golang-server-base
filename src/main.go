package src

import (
	"golang-server-base/api"
)

/*
Required

This is your application's entry point, use this as your main function
when initializing your app.

The server will be automatically launched, there is no need to
launch it from here.

Params:
`server`: The server obect, this allows you to modify properties of the server before it is launched
*/
func Main(server *api.Server) {

}
